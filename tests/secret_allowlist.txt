# Exact literals the secret scanner may ignore. One per line, each with a
# one-line reason. A WILDCARD ENTRY IS A REVIEW REJECT -- it disables the
# gate for a whole shape of value rather than for one known-safe string.
#
# Kept short deliberately: a gate that cries wolf in week two is a gate
# somebody disables in week three, and the file is only useful while it is
# small enough that additions actually get read.
#
# (no entries yet)
